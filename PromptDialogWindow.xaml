<Window x:Class="Rounder.Windows.PromptDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="420" SizeToContent="Height"
        ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" ThemeMode="System">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptLabel" Margin="0,0,0,8"/>
        <TextBox x:Name="ValueBox" Grid.Row="1" MinWidth="360" Margin="0,0,0,16"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True" MinWidth="88" Margin="0,0,8,0"/>
            <Button Content="OK" IsDefault="True" MinWidth="88" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
